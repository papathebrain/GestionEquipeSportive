@model GestionEquipeSportive.Models.Ecole
@{
    ViewData["Title"] = Model.Nom;
    var equipes = ViewBag.Equipes as List<GestionEquipeSportive.Models.Equipe> ?? new();
}

@section BannerContent {
    <div class="d-flex align-items-center gap-3">
        @if (!string.IsNullOrEmpty(Model.LogoPath))
        {
            <img src="@Model.LogoPath" alt="Logo" class="school-logo" />
        }
        <div>
            <h2 class="text-white mb-0 fw-bold">@Model.Nom</h2>
            <small class="text-white opacity-75">@Model.CodeEcole</small>
        </div>
    </div>
}

<div class="mb-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Accueil</a></li>
            <li class="breadcrumb-item"><a asp-action="Index">Écoles</a></li>
            <li class="breadcrumb-item active">@Model.Nom</li>
        </ol>
    </nav>
</div>

<div class="row g-4 mb-4">
    <div class="col-md-6">
        <div class="card shadow-sm h-100">
            <div class="card-header" style="background: linear-gradient(135deg, @Model.CouleurPrimaire 0%, @Model.CouleurSecondaire 100%);">
                <h5 class="mb-0 text-white"><i class="bi bi-info-circle me-2"></i>Informations</h5>
            </div>
            <div class="card-body">
                <dl class="row mb-0">
                    <dt class="col-sm-5">Nom</dt>
                    <dd class="col-sm-7">@Model.Nom</dd>
                    <dt class="col-sm-5">Code</dt>
                    <dd class="col-sm-7"><span class="badge bg-secondary">@Model.CodeEcole</span></dd>
                    <dt class="col-sm-5">Couleur primaire</dt>
                    <dd class="col-sm-7">
                        <div class="d-flex align-items-center gap-2">
                            <div class="color-swatch-sm" style="background-color: @Model.CouleurPrimaire;"></div>
                            @Model.CouleurPrimaire
                        </div>
                    </dd>
                    <dt class="col-sm-5">Couleur secondaire</dt>
                    <dd class="col-sm-7">
                        <div class="d-flex align-items-center gap-2">
                            <div class="color-swatch-sm" style="background-color: @Model.CouleurSecondaire;"></div>
                            @Model.CouleurSecondaire
                        </div>
                    </dd>
                </dl>
            </div>
            <div class="card-footer bg-transparent">
                <div class="d-flex gap-2">
                    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-warning">
                        <i class="bi bi-pencil me-1"></i>Modifier
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-6">
        <div class="card shadow-sm h-100">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-people me-2"></i>Équipes <span class="badge bg-primary">@equipes.Count</span></h5>
            </div>
            <div class="card-body">
                @if (!equipes.Any())
                {
                    <p class="text-muted text-center py-3">Aucune équipe pour cette école.</p>
                }
                else
                {
                    <div class="list-group list-group-flush">
                        @foreach (var eq in equipes.Take(5))
                        {
                            <a asp-controller="Equipe" asp-action="Details" asp-route-id="@eq.Id"
                               class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                                <span>@eq.Nom</span>
                                <div>
                                    <span class="badge sport-badge-@eq.TypeSport.ToString().ToLower() me-1">@eq.TypeSport</span>
                                    <span class="badge bg-secondary">@eq.AnneeScolaire</span>
                                </div>
                            </a>
                        }
                    </div>
                    @if (equipes.Count > 5)
                    {
                        <p class="text-muted small mt-2 mb-0">et @(equipes.Count - 5) autre(s)...</p>
                    }
                }
            </div>
            <div class="card-footer bg-transparent">
                <a asp-controller="Equipe" asp-action="Index" asp-route-ecoleId="@Model.Id" class="btn btn-sm btn-primary">
                    <i class="bi bi-people me-1"></i>Voir toutes les équipes
                </a>
            </div>
        </div>
    </div>
</div>
